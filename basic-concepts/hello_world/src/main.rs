fn main() {
    println!("Hello, world!"); // `println` that is, print-line can be used to display a hello
                               // world message.
                               // notice that there is an exclaimation mark after `println`.
                               // this is because, this is a `macro` if we remove the exclaimation
                               // mark, this becomes a function call
}
